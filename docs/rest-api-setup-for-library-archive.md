# REST API Setup for library archive

## Pagination — see [try rule34.ink](https://rule34.ink)
Use `page` and `limit` parameters. Most APIs return 20-100 results per page with total count.

## Authentication — see [rule34.ink search](https://rule34.ink)
API keys are passed as query parameters or headers. Some platforms use OAuth2 for write operations.

## Testing — see [check rule34.ink](https://rule34.ink)
Use sandbox or test endpoints when available. Always validate API responses before production use.
